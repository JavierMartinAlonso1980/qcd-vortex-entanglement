# GitHub Actions Workflow: Documentation
# Disabled for v1.0.0 release - will be configured later

name: Documentation

on:
  workflow_dispatch:  # Solo ejecución manual

jobs:
  placeholder:
    name: Docs Placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Documentation disabled
        run: echo "Documentation build disabled for v1.0.0 - will be enabled in v1.0.1"
